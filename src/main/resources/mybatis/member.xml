<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace = "dev.mvc.member.MemberDAOInter">
  <select id="checkID" resultType="int" parameterType="String">
    SELECT COUNT(id) as cnt
    FROM member
    WHERE id=#{id}
  </select>
  
  <select id="checkEMAIL" resultType="int" parameterType="String">
    SELECT COUNT(email) as cnt
    FROM member
    WHERE email=#{email}
  </select>
  
  <select id="checkALIAS" resultType="int" parameterType="String">
    SELECT COUNT(alias) as cnt
    FROM member
    WHERE alias=#{alias}
  </select>
  
  <insert id="create" parameterType="dev.mvc.member.MemberVO">
    INSERT INTO member(memberno, id, email, alias, passwd, mname, job, tel, zipcode, address1, address2, mdate, grade)
    VALUES (member_seq.nextval, #{id}, #{email}, #{alias}, #{passwd}, #{mname}, #{job}, #{tel}, #{zipcode}, 
                 #{address1}, #{address2}, sysdate, #{grade})
  </insert>
  
  <select id="list" resultType="dev.mvc.member.MemberVO">
    SELECT memberno, id, passwd, mname, tel, zipcode, address1, address2, mdate, grade
    FROM member
    ORDER BY grade ASC, id ASC
  </select>
     
</mapper> 